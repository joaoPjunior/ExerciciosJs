let nome = "João";

console.log(nome.length);

let numeros = [1, 2, 3, 4, 5, 6, 7];

console.log(numeros.length);
console.log(numeros["length"]);
console.log(numeros[1]);/* tudo que vemos depois de um ponto ou até mesmo um array depois de uma variavel, se considera uma "propriedade" */